import { Button } from "../ui/button";

function AddToCart() {
  return (
    <Button className="capitalize mt-8" size="lg">
      Add to Cart
    </Button>
  );
}
export default AddToCart;

// TODO { productId }: { productId: string }
